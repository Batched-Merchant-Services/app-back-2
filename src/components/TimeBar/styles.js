import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  bar: {
    borderWidth: 1,
  },
  bg      : { width: '100%', height: '100%' },
  progress: {
    borderRadius: 5
  },
  gradient: { flex: 1 }
});

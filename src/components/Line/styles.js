import { StyleSheet } from 'react-native';
import Colors from '@styles/Colors';

export default StyleSheet.create({
  line: {
    width          : '100%',
    backgroundColor: Colors.textBlue01
  }
});
